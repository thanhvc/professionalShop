/*
 describe( 'AppCtrl', function() {
 describe( 'isCurrentUrl', function() {
 var AppCtrl, $location, $scope, $httpBack;

 beforeEach( module( 'ngMo' ) );

 beforeEach( inject( function( $controller, _$location_, $rootScope, $httpBackend  ) {
 $location = _$location_;
 $scope = $rootScope.$new();
 $httpBack = $httpBackend;
 jasmine.log($httpBack);
 AppCtrl = $controller( 'AppCtrl', { $location: $location, $scope: $scope });
 }));


 it( 'should pass a dummy test', inject( function() {
 $httpBack.expectGET('i18n/lang-es.json').respond([]);
 expect( AppCtrl ).toBeTruthy();
 }));
 });
 });
 */
